
// You don't necessarily have to use axios, you can use got, fetch or any other modules you like
const axios = require('axios');


async function scrape(titles, malID, episode) {
    var streams = [];
    // Do the magic here, write code to crawl your favorite site and get data from there
    // Makse sure to push the data to streams
    return streams;
}





module.exports = scrape;